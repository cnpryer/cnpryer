# Visit https://github.com/lowlighter/metrics/blob/master/action.yml for full reference
name: Metrics
on:
  schedule: [{cron: "0 */6 * * *"}]
  # Lines below let you run workflow manually and on each commit
  push: {branches: ["master", "main"]}
  workflow_dispatch:
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          token: ${{ secrets.METRICS_TOKEN }}
          # GITHUB_TOKEN is a special auto-generated token restricted to current repository, which is used to push files in it
          committer_token: ${{ secrets.GITHUB_TOKEN }}

          # Options
          user: pryborg
          template: classic
          base: ""
          config_animated: yes
          config_timezone: America/New_York
          plugin_languages: yes
          plugin_languages_ignored: html, css, Jupyter Notebook, shell
          plugin_languages_colors: github
          plugin_languages_details: bytes-size, percentage
          plugin_stars: yes
          plugin_stars_limit: 3
          repositories_affiliations: ""
          filename: github-metrics.svg
  vti:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          token: ${{ secrets.METRICS_TOKEN }}
          # GITHUB_TOKEN is a special auto-generated token restricted to current repository, which is used to push files in it
          committer_token: ${{ secrets.GITHUB_TOKEN }}

          # Options
          user: pryborg
          base: ""
          template: classic
          config_animated: yes
          config_timezone: America/New_York
          plugin_stock: yes
          plugin_stock_token: ${{ secrets.STOCK_TOKEN }}
          plugin_stock_symbol: VTI
          plugin_stock_duration: 1d
          plugin_stock_interval: 6m
          filename: vti.svg
  slv:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          token: ${{ secrets.METRICS_TOKEN }}
          # GITHUB_TOKEN is a special auto-generated token restricted to current repository, which is used to push files in it
          committer_token: ${{ secrets.GITHUB_TOKEN }}

          # Options
          user: pryborg
          base: ""
          template: classic
          config_animated: yes
          config_timezone: America/New_York
          plugin_stock: yes
          plugin_stock_token: ${{ secrets.STOCK_TOKEN }}
          plugin_stock_symbol: SLV
          plugin_stock_duration: 1d
          plugin_stock_interval: 6m
          filename: slv.svg
  eth-usd:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          token: ${{ secrets.METRICS_TOKEN }}
          # GITHUB_TOKEN is a special auto-generated token restricted to current repository, which is used to push files in it
          committer_token: ${{ secrets.GITHUB_TOKEN }}

          # Options
          user: pryborg
          base: ""
          template: classic
          config_animated: yes
          config_timezone: America/New_York
          plugin_stock: yes
          plugin_stock_token: ${{ secrets.STOCK_TOKEN }}
          plugin_stock_symbol: ETH-USD
          plugin_stock_duration: 1d
          plugin_stock_interval: 6m
          filename: eth-usd.svg
  ada-usd:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          token: ${{ secrets.METRICS_TOKEN }}
          # GITHUB_TOKEN is a special auto-generated token restricted to current repository, which is used to push files in it
          committer_token: ${{ secrets.GITHUB_TOKEN }}

          # Options
          user: pryborg
          base: ""
          template: classic
          config_animated: yes
          config_timezone: America/New_York
          plugin_stock: yes
          plugin_stock_token: ${{ secrets.STOCK_TOKEN }}
          plugin_stock_symbol: ADA-USD
          plugin_stock_duration: 1d
          plugin_stock_interval: 6m
          filename: ada-usd.svg
